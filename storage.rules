rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow read and write access for all authenticated users.
    // This is not recommended for production, but is useful for development.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
